# USAJobs Search API — used only by the Vite dev server proxy (never bundled).
# Request a key: https://developer.usajobs.gov/
# User-Agent must be a contact email per https://developer.usajobs.gov/General/Quick-Start
USAJOBS_API_KEY=
USAJOBS_USER_AGENT=your.email@example.com
